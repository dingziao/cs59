{
  "name": "elm-basic-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npx elm make src/Main.elm --output=elm.js",
    "serve": "npx http-server -c-1 . -p 8000",
    "clean": "rm elm.js"
  },
  "devDependencies": {
    "elm": "^0.19.1-3",
    "http-server": "^14.1.1"
  }
}
